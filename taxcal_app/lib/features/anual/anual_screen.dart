import 'package:flutter/material.dart';

/// Construido en la Fase 3 del plan de implementación.
class AnualScreen extends StatelessWidget {
  const AnualScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Anual — próximamente')));
  }
}
